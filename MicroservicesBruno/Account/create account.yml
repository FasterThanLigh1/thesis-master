info:
  name: create account
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/auth/accounts"
  body:
    type: json
    data: |-
      {
        "name": "John",
        "email": "johnny@gmail.com",
        "age": 10
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
