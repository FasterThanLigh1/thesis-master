info:
  name: get accounts
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/auth/accounts"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
