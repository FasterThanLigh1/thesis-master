info:
  name: update partial
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{BASE_URL}}/auth/accounts/1"
  body:
    type: json
    data: |-
      {
        "name": "Jill"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
